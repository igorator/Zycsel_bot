const { SCREENS } = require('./screens');
const { KEYBOARDS } = require('./keyboards');
const { ITEMS } = require('./items');
const { TABLES } = require('./tables');

module.exports = { SCREENS, KEYBOARDS, ITEMS, TABLES };
